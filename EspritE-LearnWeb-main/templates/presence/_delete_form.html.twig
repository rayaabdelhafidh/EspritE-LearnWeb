<form method="post" action="{{ path('app_presence_delete', {'idpresence': presence.idpresence}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presence.idpresence) }}">
    <button class="btn">Delete</button>
</form>
