<form method="post" action="{{ path('app_classe_delete', {'idclasse': classe.idclasse}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe.idclasse) }}">
    <button class="btn">Delete</button>
</form>
