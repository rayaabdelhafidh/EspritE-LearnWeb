{% extends 'baseback.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdClasse</th>
                <td>{{ classe.idClasse }}</td>
            </tr>
            <tr>
                <th>Nomclasse</th>
                <td>{{ classe.nomClasse }}</td>
            </tr>
            <tr>
                <th>Filiere</th>
                <td>{{ classe.filiere }}</td>
            </tr>
            <tr>
                <th>Nbreetudi</th>
                <td>{{ classe.nbreetudi }}</td>
            </tr>
            <tr>
                <th>Niveaux</th>
                <td>{{ classe.niveaux }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classe_index') }}">back to list</a>

    <a href="{{ path('app_classe_edit', {'idClasse': classe.idClasse}) }}">edit</a>

    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
