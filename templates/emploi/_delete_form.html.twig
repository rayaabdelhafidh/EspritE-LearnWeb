<form method="post" action="{{ path('app_emploi_delete', {'id': emploi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emploi.id) }}">
    <button class="btn" style="color: red;">supprimer</button>
</form>
