<form method="post" action="{{ path('app_matiere_delete', {'idm': matiere.idm}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matiere.idm) }}">
    <button class="btn">Delete</button>
</form>
