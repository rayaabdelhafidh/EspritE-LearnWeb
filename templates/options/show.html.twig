{% extends 'base.html.twig' %}

{% block title %}Options{% endblock %}

{% block body %}
    <h1>Options</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>OptionId</th>
                <td>{{ option.optionId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_options_index') }}">back to list</a>

    <a href="{{ path('app_options_edit', {'optionId': option.optionId}) }}">edit</a>

    {{ include('options/_delete_form.html.twig') }}
{% endblock %}
