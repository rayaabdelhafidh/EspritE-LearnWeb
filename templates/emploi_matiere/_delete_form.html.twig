<form method="post" action="{{ path('app_emploi_matiere_delete', {'emploi': emploi_matiere.emploi}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emploi_matiere.emploi) }}">
    <button class="btn">Delete</button>
</form>
