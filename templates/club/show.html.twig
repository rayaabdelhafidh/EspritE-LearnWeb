{% extends 'base.html.twig' %}

{% block title %}Club{% endblock %}

{% block body %}
    <h1>Club</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idclub</th>
                <td>{{ club.idclub }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_club_index') }}">back to list</a>

    <a href="{{ path('app_club_edit', {'idclub': club.idclub}) }}">edit</a>

    {{ include('club/_delete_form.html.twig') }}
{% endblock %}
