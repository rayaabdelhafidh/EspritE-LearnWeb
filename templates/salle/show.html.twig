{% extends 'baseback.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>salleid</th>
                <td>{{ salle.salleId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salle_index') }}">back to list</a>

    <a href="{{ path('app_salle_edit', {'salleid': salle.salleId}) }}">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
