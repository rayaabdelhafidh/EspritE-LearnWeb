<form method="post" action="{{ path('app_salle_delete', {'salleId': salle.salleId}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle.salleId) }}">
    <button class="btn" style="color: red;">Supprimer</button>
</form>

