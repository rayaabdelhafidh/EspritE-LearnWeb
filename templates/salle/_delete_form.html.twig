<form method="post" action="{{ path('app_salle_delete', {'salleid': salle.salleid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle.salleid) }}">
    <button class="btn">Delete</button>
</form>
