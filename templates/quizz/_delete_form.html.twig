<form method="post" action="{{ path('app_quizz_delete', {'quizId': quizz.quizId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quizz.quizId) }}">
    <button class="btn">Delete</button>
</form>
