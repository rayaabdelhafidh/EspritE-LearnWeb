{% extends 'base.html.twig' %}

{% block title %}Quizz{% endblock %}

{% block body %}
    <h1>Quizz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>QuizId</th>
                <td>{{ quizz.getQuizId() }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quizz_index') }}">back to list</a>

    <a href="{{ path('app_quizz_edit', {'quizId': quizz.getQuizId()}) }}">edit</a>
   

    {{ include('quizz/_delete_form.html.twig') }}
{% endblock %}
