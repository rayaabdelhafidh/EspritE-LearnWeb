
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emploi</title>
    <style>
        @font-face {
            font-family: 'Roboto', sans-serif;
            src: local('Roboto'), url('path/to/Roboto.woff2') format('woff2');
        }

        html, body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            background-color: #d4d4d4; /* New background color */
            color: #1f1f1f; /* New text color */
        }

        h1 {
            text-align: center;
            padding: 20px 0;
            font-size: 22px;
            font-weight: bold;
            color: #b90000; /* Lighter red */
        }

        table {
            width: 100%;
            border-collapse: collapse;
            page-break-inside: avoid;
        }

        th, td {
            border: 1px solid #444;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #333;
            color: #731010; /* New text color for column headers */
        }

        tr:nth-child(even) {
            background-color: #444;
        }

        .table-responsive {
            overflow-x: auto;
        }

        .text-center {
            text-align: center;
        }

        /* Add more styles as needed */
    </style>
</head>
<body>
  {% for question in questions %}
        <div>
            <p style="display: inline-block; margin-right: 300px;">{{ question.getContent() }}</p>
            <p style="display: inline-block;">Score: {{ question.getScore() }}</p>
            <ul>
                {% if question.getOptions() is not empty %}
                    {% for option in question.getOptions() %}
                        <li>
                            <input type="radio" id="{{ option.getOptionId() }}" name="question{{ question.getQuestionId() }}" value="{{ option.getOptionId() }}">
                            <input type="hidden" name="questionId{{ option.getOptionId() }}" value="{{ question.getQuestionId() }}">
                            <label for="{{ option.getOptionId() }}">{{ option.getOptionContent() }}</label>
                        </li>
                    {% endfor %}
                {% else %}
                    <li>No options available</li>
                {% endif %}
            </ul>
        </div>
    {% endfor %}
        
</body>
</html>
